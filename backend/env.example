# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/pecha_annotation_db

# Auth0 Configuration
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_AUDIENCE=your-api-audience-identifier

# Development Settings
DEBUG=true
LOG_LEVEL=info

# CORS Settings (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Optional: Auth0 Cache Settings (for production optimization)
# AUTH0_CACHE_TTL=3600  # Cache JWKS for 1 hour
# AUTH0_REQUEST_TIMEOUT=10  # Request timeout in seconds 